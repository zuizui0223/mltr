# Python build and cache artifacts
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Deterministic replay and figure outputs.
# Regenerated by scripts/verify_transport_core.py,
# scripts/write_submission_figure_data.py, and
# scripts/render_submission_figures.py, and rebuilt in CI by the
# transport-reproducibility and manuscript-build workflows.
# Pin these to a release or attach them to the submission commit rather
# than tracking them on a working branch.
artifacts/

# LaTeX build products
manuscript/*.pdf
manuscript/*.aux
manuscript/*.bbl
manuscript/*.blg
manuscript/*.fdb_latexmk
manuscript/*.fls
manuscript/*.log
manuscript/*.out
manuscript/*.toc
